import { defineConfig } from "@apps-in-toss/web-framework/config";

export default defineConfig({
  appName: "in-app-payments",
  brand: {
    primaryColor: "#3182F6",
  },
  webView: {},
  permissions: [],
  webBundleDir: "dist",
});
