import { defineConfig } from "@apps-in-toss/web-framework/config";

export default defineConfig({
  appName: "data-sdk",
  brand: {
    primaryColor: "#0F8F7E",
  },
  webView: {},
  permissions: [],
  webBundleDir: "dist",
});
