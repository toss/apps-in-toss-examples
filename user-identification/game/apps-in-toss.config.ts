import { defineConfig } from "@apps-in-toss/web-framework/config";

export default defineConfig({
  appName: "user-identification-game",
  brand: {
    primaryColor: "#F04452",
  },
  webView: {},
  permissions: [],
  webBundleDir: "dist",
});
