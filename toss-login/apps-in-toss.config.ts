import { defineConfig } from "@apps-in-toss/web-framework/config";

export default defineConfig({
  appName: "toss-login",
  brand: {
    primaryColor: "#3182F6",
  },
  webView: {},
  permissions: [],
  webBundleDir: "dist",
});
